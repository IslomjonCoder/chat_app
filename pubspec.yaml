name: chat_app
description: "A new Flutter project."


publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.6.0
  intl: any
  gap: ^3.0.1
  logging: ^1.2.0
  path: ^1.9.0
  path_provider: ^2.1.4
  drift: any
  drift_flutter: ^0.1.0
  drift_sqlite_async: any
  powersync: ^1.5.0
  uuid: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.12
  drift_dev: any

flutter:
  uses-material-design: true
